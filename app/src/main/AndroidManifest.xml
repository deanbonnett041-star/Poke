<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Only used for the optional card/set identification lookup
         (api.pokemontcg.io) and the one-time OCR model download; grading
         itself never needs network access and always works offline. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.AiCardGrader"
        android:supportsRtl="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AiCardGrader">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets Play Store pre-fetch the on-device text-recognition model at
             install time when installed via Play; harmless no-op otherwise
             (e.g. sideloaded debug builds), which still download the model
             on first use over the network and run fully offline after that. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />

        <!-- Home-screen widget: a one-tap shortcut straight into the
             capture flow. Must be exported so the system launcher (a
             different app) can deliver widget-update broadcasts to it;
             that's the standard, expected exception to keeping components
             unexported. -->
        <receiver
            android:name=".widget.SlabrateWidgetProvider"
            android:exported="true"
            android:label="@string/widget_grade_card_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/slabrate_widget_info" />
        </receiver>
    </application>
</manifest>
